# Brutal Loading Screen - Syndikate

Брутальный экран загрузки с индустриальной эстетикой для покерного клуба Syndikate.

## Возможности

- 🎨 **Брутальный дизайн**: Металлические текстуры, бетон, неоновые акценты
- ✨ **Анимации**: Искры, световые блики, вращающиеся элементы
- 🔊 **Звуковые эффекты**: Опциональные индустриальные звуки (металл, искры, гул)
- 📊 **Прогресс бар**: Анимированный индикатор загрузки с сканирующей линией
- 🎯 **Респонсив**: Адаптивный дизайн для всех устройств

## Использование

### Базовое использование

```tsx
import { BrutalLoadingScreen } from "@/components/BrutalLoadingScreen";

function App() {
  const [isLoading, setIsLoading] = useState(true);

  return (
    <>
      {isLoading && (
        <BrutalLoadingScreen 
          onLoadingComplete={() => setIsLoading(false)}
        />
      )}
      {/* Ваш контент */}
    </>
  );
}
```

### С звуковыми эффектами

```tsx
<BrutalLoadingScreen 
  onLoadingComplete={() => setIsLoading(false)}
  enableSounds={true}
/>
```

## Props

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `onLoadingComplete` | `() => void` | - | Callback после завершения загрузки |
| `enableSounds` | `boolean` | `false` | Включить звуковые эффекты |

## Звуковые эффекты

Компонент использует Web Audio API для генерации индустриальных звуков:

- **Металлический звук** - при достижении 25%, 50%, 75%
- **Искры** - каждые 10% прогресса
- **Электрический гул** - фоновый звук при запуске
- **Power Up** - при завершении загрузки

Пользователь может включить/выключить звуки кнопкой в правом верхнем углу.

## Кастомизация

### Цвета

Все цвета используют токены дизайн-системы Syndikate:
- `--syndikate-orange` - основной неоновый оранжевый
- `--syndikate-red` - агрессивный красный
- `--syndikate-metal` - металлические поверхности
- `--syndikate-concrete` - бетонные текстуры

### Длительность

Экран загрузки автоматически заполняет прогресс за ~7-10 секунд. Для изменения длительности отредактируйте интервал в компоненте:

```tsx
setInterval(() => {
  // ... логика
}, 150); // Измените 150ms для другой скорости
```

## Оптимизация

- Показывается только при первом посещении (sessionStorage)
- Анимации оптимизированы для производительности
- Звуки генерируются процедурно (не требуют файлов)

## Совместимость

- React 18+
- Framer Motion 12+
- Modern browsers с Web Audio API
